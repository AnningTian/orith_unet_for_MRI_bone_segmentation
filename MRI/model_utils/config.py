import os

# Path to the folder containing the image files (PNG format) with wildcard for matching multiple files
image_path = r"D:\MIGLAB\sam2_finetune\data\datasets_v2\img\*.png"

# Path to the folder containing the mask files (PNG format) with wildcard for matching multiple files
mask_path = r"D:\MIGLAB\sam2_finetune\data\datasets_v2\mask\*.png"
